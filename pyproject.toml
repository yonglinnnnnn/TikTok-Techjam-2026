[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "truesight"
version = "0.1.0"
description = "TrueSight AI-generated image detector"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
